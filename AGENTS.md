# AI 免费模型接口监控台

## 应用概览

一个聚合 38 家 AI 平台的 API Key 管理、健康检测与用量监控仪表盘。用户可集中管理各平台 API Key，一键检测接口可用性，查看用量剩余与历史趋势。

## 设计规范

### 视觉风格
- **定位**：开发者工具 / 运维监控仪表盘，信息密度高，清爽专业
- **主色调**：`#1890ff`（科技蓝），辅以状态色
- **背景**：`#f5f7fa` 页面背景，`#ffffff` 卡片背景
- **边框**：`#e4e7ed` 浅灰分割
- **文字**：主 `#1f2937`，次 `#6b7280`，辅助 `#9ca3af`
- **状态色**：
  - 正常/可用：`#10b981`（绿）
  - 异常/错误：`#ef4444`（红）
  - 警告/额度低：`#f59e0b`（橙）
  - 未配置/停用：`#9ca3af`（灰）
  - 检测中：`#3b82f6`（蓝）

### 布局与间距
- 最大内容宽度：1280px，居中
- 页面内边距：px-6（桌面），px-4（移动端）
- 卡片圆角：`rounded-lg`（8px）
- 卡片内边距：`p-5`
- 区块间距：`gap-4` / `gap-6`
- 按钮高度：标准 `h-9`

### 排版
- 页面大标题：`text-2xl font-semibold`
- 区块标题：`text-lg font-semibold`
- 正文：`text-sm`
- 辅助文字：`text-xs text-gray-500`

## 技术架构

### 后端模块
- `platforms` 模块：平台预置数据、用户平台配置 CRUD、Key 加密存储
- `monitoring` 模块：健康检测服务（真实调用各平台 API）、检测记录持久化、定时检测
- `usage` 模块：用量统计服务、用量历史记录、导出功能

### 前端页面
- `/` 总览仪表盘：状态统计卡片 + 平台状态一览 + 今日用量概览
- `/platforms` 平台列表：38家平台卡片网格，支持搜索筛选、启用/停用、Key 配置
- `/platforms/:id` 平台详情：免费额度信息 + 当前状态 + 用量图表 + 检测历史
- `/settings` 设置：定时检测配置

### 数据模型
- `platforms`：平台预置数据（代码内常量，不存数据库）
- `user_platform_configs`：用户平台配置（API Key、是否启用、备注）
- `health_check_records`：健康检测记录（状态、延迟、错误信息）
- `usage_records`：用量历史记录（剩余额度、已用 Token、速率限制信息）

### 安全约束
- API Key 后端加密存储，前端仅返回脱敏（前4后4）
- 日志中不得输出完整 Key
- 检测请求通过后端代理发起，前端不持有完整 Key
